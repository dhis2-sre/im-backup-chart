apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "im-backup.fullname" . }}-test-connection"
  labels:
    {{- include "im-backup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "im-backup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
