# Default values for im-backup.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

environment: dev

schedule: "@every 24h"

ttl: 720h0m0s # 30 days

includedNamespaces:
  - instance-manager-dev
  - whoami

csiSnapshotTimeout: 10m
